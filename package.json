{
  "name": "lumis-minimal-backend",
  "version": "1.0.0",
  "description": "Minimal backend for Lumis Jewelry checkout",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.x.x",        # Or latest stable version
    "cors": "^2.8.5",
    "dotenv": "^16.x.x",      # Or latest stable version
    "express": "^4.18.x",     # Or latest stable version
    "nodemailer": "^6.x.x"    # Or latest stable version
  },
  "devDependencies": {
    "nodemon": "^3.x.x"       # Or latest stable version
  }
}